import { NodeLabelDirectionType, OppositeNodes } from '../../common/configs';
import { Position } from '../../common/types';
export declare function handleNodeLabelAutoAdjustment(nodeId: string, currentPos: Position, oppositeNodes: OppositeNodes, getLoopCenter: (edgeId: string) => Position | undefined, defaultDirection: NodeLabelDirectionType): NodeLabelDirectionType;
