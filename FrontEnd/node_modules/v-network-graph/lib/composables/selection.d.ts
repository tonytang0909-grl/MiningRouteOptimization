import { Reactive } from '../common/common';
interface Selections {
    selectedNodes: Reactive<Set<string>>;
    selectedEdges: Reactive<Set<string>>;
    selectedPaths: Reactive<Set<string>>;
}
export declare function provideSelections(selectedNodes: Reactive<Set<string>>, selectedEdges: Reactive<Set<string>>, selectedPaths: Reactive<Set<string>>): void;
export declare function useSelections(): Selections;
export {};
